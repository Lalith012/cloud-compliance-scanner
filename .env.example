# AWS Configuration
# Copy this file to .env and fill in your values
# .env is gitignored — never commit it

AWS_PROFILE=member
AWS_REGION=ap-south-1
AWS_ACCOUNT_ID=664858858896

# Set to "true" to push findings to Security Hub
# Keep "false" during local development to avoid noise
PUSH_TO_HUB=false